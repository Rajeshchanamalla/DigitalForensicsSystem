# Skip optional platform-specific packages (e.g. fsevents which is macOS-only)
# This prevents npm ci from failing on Railway (Linux) when package-lock.json
# was generated on Windows/macOS and includes darwin-only optional packages.
omit=optional
